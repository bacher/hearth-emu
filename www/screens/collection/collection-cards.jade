
each card in cards
   .card-wrapper
      .card(data-id=card.id,class=card.flags['unique'] ? 'unique' : '')
         if (card.loaded)
            IMG.img.front(src='http://media-hearth.cursecdn.com/avatars/' + card.pic + '.png')

         .glow
