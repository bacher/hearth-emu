
each card in cards
   .card-wrapper
      .card(data-id= card.id,
         class= (card.flags['unique'] ? 'unique' : ''))

         if (card.loaded)
            IMG.img.front(src=H.makeCardUrl(card.pic))

         .glow

         if (card.flags['unimplemented'])
            .unimplemented Not implemented yet
